import OrderComplete from "../components/OrderComplete/OrderComplete";

const orderComplete = () => {
    return (
        <div>
            <OrderComplete/>
        </div>
    );
};

export default orderComplete;